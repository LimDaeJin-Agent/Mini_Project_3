# (서비스 이름)
냉장고 파먹기
## 0. 기본 정보
- 이름 : 냉장고 파먹기
- 배포 주소 : https://fridge-recipe-omega.vercel.app
- 사용한 AI 모델/API :GEMINI API

## 1. 한 줄 소개
냉장고 안에 부족한 재료를 활용하여 만들고 싶은 요리나 주재료를 통하여 어떤 요리를 만들어 먹을지 추천해주는 서비스입니다.

## 2. 이런 사람을 위해 만들었습니다
- 대상 : 자취생, 가정주부 등
- 그 사람이 지금 겪는 불편 : 남은 식재료를 사용하기 위해 또다른 식재료가 남게 되는 불편이 발생됨
- 이 서비스를 쓰면 달라지는 것 : 남은 식재료를 사용하기 위해 부족한 식재료를 충당하돼 알맞는 양만 충당 및 소비하게되므로 남는 재료를 없애거나 최소화 시킬수 있음

## 3. 주요 기능 (3개 이내)
- 단순한 남는 식재료와 먹고싶은 요리를 입력하면 관련된 요리 목록 및 레시피를 보여줌(화기 사용시 타이머 기능 제공)
- 주재료만 입력하면 지역의 기온 및 날씨와 연동하여 적절한 추천 메뉴를 제공함
- 저장 기능을 통해 과거 사용했던 메뉴 및 레시피를 저장, 삭제 기능을 이용하여 필요할때 레시피를 다시 불러 쓸수 있음

## 4. AI를 어디에 어떻게 썼나요
- AI가 하는 일 : 남는 음식재료를 활용하기 위한 날씨에 따른 메뉴 추천
- 입력으로 무엇을 주고, 무엇을 받나요 : 음식 재료를 입력 받고 레시피를 제공함
- AI가 이상한 답을 줬을 때 어떻게 처리했나요 : 
- 프롬프트에서 신경 쓴 점 1가지 : 지역 온도별 제공 되는 메뉴의 차별화

## 5. 데이터 설계
- 테이블 이름과 역할
  recipes — 사용자가 "저장하기"를 누른 레시피만 영구 저장
  search_history — "저장" 여부와 무관하게 모든 추천 요청과 결과를 자동 기록 (검색 이력)
  status_checks — /api/health 호출 시 Next.js·Supabase 상태 점검 결과 기록
- 주요 컬럼
  recipes: ingredients_input(입력 재료), recipe_name, cook_time, servings_requested, main_ingredients/seasonings(jsonb — 재료명·양·손질법·초벌여부·투입순서 포함), steps(jsonb — 단계별 설명 + 가스/인덕션별 소요시간), cooking_device
  search_history: ingredients_input, desired_dish, weather_condition/weather_temp/weather_location, results(jsonb — 추천된 요리명·이유·조리시간)
  status_checks: nextjs_ok, supabase_ok, supabase_error
- 이렇게 설계한 이유
  "저장한 레시피"와 "검색 이력"을 분리한 이유는, 사용자가 마음에 안 들어서 저장하지 않은 추천도 이력으로는 남겨서 나중에 추천받은 메뉴에 대해 확인하기 위함.
  재료·조리단계처럼 개수가 가변적이고 하위 속성이 많은 데이터는 각각 별도 테이블로 쪼개는 대신 jsonb 컬럼 하나에 배열로 저장.
  이 프로젝트 규모에서는 조인 없이 한 번에 읽고 쓰는 게 더 단순하고, 재료 하나하나를 다른 테이블에서 검색/집계할 일이 없기 때문.
  로그인 기능이 없는 프로젝트라 단순하게 구성했습니다

## 6. 화면 흐름
(사용자가 들어와서 무엇을 보고, 무엇을 누르고, 무엇을 얻는지 순서대로)
1. 홈 화면(/) 접속 → 무엇을 입력해야 하는지 안내 문구를 보고, 재료/먹고 싶은 요리/조리시간/인분 입력칸을 확인한다
2. (선택) 📍 내 위치 날씨 버튼을 눌러 현재 위치와 기온·날씨를 확인한다
3. 재료(또는 먹고 싶은 요리)를 입력하고 레시피 추천받기를 누른다 → 메모지 넘어가는 로딩 애니메이션을 보며 기다린다 (원하면 정지로 취소)
4. 요리 이름 + 추천 이유만 적힌 카드 목록을 받는다
5. 마음에 드는 요리 이름을 클릭해 펼쳐서, 재료표(1인분/요청인분)·손질 및 투입 순서·조리 단계를 확인한다
6. 조리 단계의 ⏱ 타이머를 눌러 실제로 시간을 재고, 끝나면 알림음으로 확인한다
7. 마음에 든 레시피는 저장하기를 눌러 DB에 저장한다
8. 저장한 레시피 보기로 이동해 목록(접힌 상태)을 보고, 이름을 눌러 다시 펼쳐보거나, 필요 없는 건 삭제(개별 🗑 또는 길게 눌러 여러 개 선택 삭제)한다

## 7. 기획서와 달라진 점
- 뺀 것
  냉장고 재고 관리(재료 등록/차감, 유통기한 추적) 기능
  사진으로 냉장고를 찍어 재료를 자동 인식하는 기능 (텍스트 직접 입력만 지원)
  저장된 레시피를 수정하는 기능 (저장/삭제만 가능)
  
- 뺀 이유
  이 서비스의 핵심 기능(메뉴 추천 및 레시피)와 거리가 있는 기능은 처음부터 제외함
  텍스트 입력으로 핵심 흐름(추천→저장→재확인)을 확실히 완성한 뒤 여유가 있으면 추가하려함
- 시간이 더 있었다면 넣었을 것 : 모바일 사진 첨부 기능(사진으로 식재료 및 유통기한 자동 인식), 추후 유통기한이 지나기 전 알람을 발생, 관련 메뉴 및 레시피 제공

## 8. 만들면서 가장 오래 막혔던 것
- 문제 : 
- 어떻게 해결했나 :

---

## UPDATE (2026-08-10) — 음성 비서 기능 추가

### 배경
요리할 때는 손에 재료나 물이 묻어 있어서 화면을 터치하기 어렵다는 점에 착안해서, 말로 묻고 들을 수 있는 핸즈프리 기능을 추가했습니다.

### 추가된 기능
1. **음성 인식 (말의 의도 파악)** — `gemini-3.1-pro-preview`
   - 마이크가 항상 켜져 있다가(상시 대기), 사용자가 말을 시작하면 자동으로 녹음을 시작하고 말이 끝나면(약 0.8초 무음) 자동으로 녹음을 종료해서 서버로 전송합니다. 버튼을 누를 필요가 없습니다.
   - 알아듣는 명령:
     - "재료를 불러줘" → 지금 펼쳐본 레시피의 주재료·양념을 하나씩 읽어줌
     - "레시피를 알려줘" → 조리 순서를 단계별로 읽어줌
     - "그만" / "멈춰" → 읽어주는 것을 즉시 멈춤
     - "처음부터 다시 불러줘" → 읽기를 처음부터 다시 시작
     - "이어서 불러줘" → 멈췄던 위치부터 계속 읽음
     - **"두부, 고추장, 그리고 뭐라고?"** 같은 패턴 → 사용자가 말한 재료(두부, 고추장)를 레시피의 실제 재료 목록과 대조해서 정확히 매칭하고, 아직 언급하지 않은 나머지 재료만 읽어줌 (재료를 하나씩 넣으면서 "다음에 뭘 넣어야 하지?"를 물어보는 실사용 시나리오를 가정)
   - 단순 받아쓰기(STT)가 아니라, 오디오를 직접 Gemini에 보내서 "무슨 명령인지 + 어떤 재료를 말했는지"까지 한 번에 판단하도록 구조화된 응답(JSON 스키마)으로 받습니다.

2. **음성 생성 (문장을 소리로)** — `gemini-2.5-pro-preview-tts`
   - 위 명령에 대한 응답과, 조리 타이머 안내를 실제 음성으로 읽어줍니다.
   - 조리 타이머 연동: 타이머를 시작하면 "N분간 가열을 시작합니다" → 남은 시간이 1분/30초/10초가 되는 순간 "1분 남았습니다"/"30초 남았습니다"/"10초 남았습니다" → 종료 시 "완료되었습니다"까지 자동으로 안내합니다.
   - Gemini가 반환하는 오디오는 헤더 없는 raw PCM(24kHz, 16bit)이라, 서버에서 WAV 헤더를 직접 붙여 브라우저에서 바로 재생 가능한 형태로 변환합니다.
   - 매번 같은 고정 문구(예: "1분 남았습니다")를 새로 생성하면 느리고 비용도 들기 때문에, 한 번 생성한 음성은 클라이언트에서 캐시해두고 재사용합니다.

3. **화면 연동**
   - 레시피 카드를 펼치면(홈 화면·저장한 레시피 화면 모두) 그 레시피가 "지금 음성 비서가 알고 있는 레시피"로 지정됩니다(React Context로 전역 공유).
   - 음성 비서 자체는 화면에 아무것도 그리지 않는 백그라운드 컴포넌트로, 앱 전체(레이아웃)에 항상 떠 있습니다.

### 기술적으로 신경 쓴 점
- **마이크 권한이 없거나 거부된 경우**에도 앱이 깨지지 않도록, 에러를 잡아서 화면 구석에 작은 안내만 표시하고 나머지 기능은 그대로 동작하게 처리했습니다.
- **음성 생성 실패에도 안전하게 동작**: `gemini-2.5-pro-preview-tts`는 프리뷰(베타) 모델이라 간헐적으로 빈 응답이나 500 에러를 반환하는 걸 실제로 확인했습니다. 자동 재시도(지연 시간을 두고 최대 4회)를 넣었고, 그래도 실패하면 그 순간의 음성 안내만 조용히 생략되고 타이머·화면 등 나머지 기능에는 영향이 없도록 만들었습니다.
- 실제 마이크 없이도 테스트할 수 있도록, TTS로 문장을 음성으로 만든 뒤 그 음성을 다시 음성 인식 API에 입력으로 넣어서 전체 파이프라인(음성 생성 → 음성 인식 → 의도 파악)을 검증했습니다.

### 알려진 한계
- 두 모델 모두 구글의 프리뷰(베타) 단계라 가끔 응답이 불안정합니다 (저희 쪽 코드 문제가 아니라 구글 서버 쪽 이슈로 확인).
- 음성 비서가 말하는 동안에도 마이크가 계속 켜져 있어서, 스피커 소리가 마이크로 다시 들어가는 에코(자기 음성 인식) 문제는 별도로 처리하지 않았습니다. 이어폰 사용을 권장합니다.
- 상시 듣기 방식이라 주변 소음이 크면 잘못 녹음이 시작될 수 있습니다.

### 배포
- 코드는 GitHub(`Mini_Project_3`)에 커밋/푸시 완료, Vercel 프로덕션에도 배포 완료: https://fridge-recipe-omega.vercel.app
